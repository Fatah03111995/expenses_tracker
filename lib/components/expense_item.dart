import 'package:flutter/material.dart';

class ExpenseItem extends StatelessWidget {
  final Expense expenseDataItem;
  const ExpenseItem({super.key, required this.expenseDataItem});

  @override
  Widget build(BuildContext context) {
    return Card();
  }
}
